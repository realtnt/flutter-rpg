name: flutter_rpg
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  cloud_firestore: ^4.17.2
  envied: ^0.5.4+1
  firebase_core: ^2.30.1
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  path_provider: ^2.1.3
  provider: ^6.1.2
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  envied_generator: ^0.5.4+1
  build_runner: ^2.4.9

flutter:
  uses-material-design: true
  assets:
    - assets/img/vocations/
    - assets/img/skills/